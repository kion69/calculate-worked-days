export interface IDateInput {
    AmountMinutes: number,
    AmountPause: number,
    BeginWork: string,
    DateGrid: string,
    EndWork: string,
    ID: number,
    LunchEndTime: string,
    LunchStartTime: string,
    TaskAmountMinutes: number,
    UserName: string,
}